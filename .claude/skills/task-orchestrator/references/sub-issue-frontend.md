# [Frontend] <screen or behavior>

> Sub-issue of `<NN>-<slug>`. Label: `frontend`.

## Requirements

<!-- Numbered and checkable. State what the user observes, including the three intentional states (loading, error-with-retry, empty) whenever a view is query-backed, and how server state flows (TanStack Query keys, typed openapi-fetch client from @sezzle/api-contract). -->

1. <requirement>
2. <requirement>

## Implementation plan

<!-- The decisions, not a tutorial: components, hooks, where keys live, what the unit test proves. -->

- `apps/web/src/...` — <what lands here>.
- Test: <the util or behavior the unit test must prove>.
- Verify: <the user flow to drive in the browser and what must be visible>.
